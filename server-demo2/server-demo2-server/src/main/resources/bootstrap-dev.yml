spring:
  cloud:
    config:
      label: master
      profile: dev
      name: server-demo2
      discovery:
        service-id: config-server
        enabled: true
      username: admin
      password: admin